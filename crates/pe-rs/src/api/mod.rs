//! The **outer layer** of the library: capability traits that a GUI (or any
//! consumer) drives directly. Implementations operate on [`crate::domain::PeDocument`].
//!
//! Filled in during Phase 1 (outer API + domain types).
